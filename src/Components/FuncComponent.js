// * BUILDING A FUNCTIONAL COMPONENT * //

// Step One: import React from 'react'
import React from 'react';

// Step Two: Create your component with a JS function
const FuncComponent = () => {
    // Step Three: Return some JSX (this is REQUIRED)
    return (
        <section>Functional Component</section>
    )
}

// Step Four: Export your component so it can be used in your application
export default FuncComponent;